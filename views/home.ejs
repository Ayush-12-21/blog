<!-- <!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
    <title>Homepage</title>
  </head>
  <body>
    <%- include('./partials/nav') %>

    <div class="container mt-3">
      <div class="row row-cols-4">
        <% blogs.forEach(blog => { %>
        <div class="col">
          <div class="card col" style="width: 18rem">
            <img src="<%= blog.coverImageURL %>" class="card-img-top" />
            <div class="card-body">
              <h5 class="card-title"><%= blog.title %></h5>
              <a href="/blog/<%= blog._id %>" class="btn btn-primary">View</a>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
    </div>

    <%- include('./partials/scripts') %>
  </body>
</html> -->


<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
    <title>Blog Homepage</title>
    <style>
      :root {
        --primary-color: #4361ee;
        --primary-hover: #3a56d4;
        --text-color: #333;
        --light-bg: #f8f9fa;
        --card-shadow: 0 10px 20px rgba(0, 0, 0, 0.08);
        --card-shadow-hover: 0 15px 30px rgba(0, 0, 0, 0.12);
      }
      
      body {
        background-color: #f8f9fa;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        padding-top: 76px; /* Account for fixed navbar */
      }
      
      .page-header {
        text-align: center;
        margin: 3rem 0;
        padding: 0 1rem;
      }
      
      .page-header h1 {
        font-weight: 700;
        color: #212529;
        margin-bottom: 0.5rem;
      }
      
      .page-header p {
        color: #6c757d;
        font-size: 1.1rem;
        max-width: 700px;
        margin: 0 auto;
      }
      
      .blogs-container {
        padding: 0 1rem 3rem;
      }
      
      .blog-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 2rem;
        margin: 0 auto;
        max-width: 1400px;
      }
      
      .blog-card {
        background: white;
        border-radius: 12px;
        overflow: hidden;
        transition: all 0.3s ease;
        box-shadow: var(--card-shadow);
        height: 100%;
        display: flex;
        flex-direction: column;
      }
      
      .blog-card:hover {
        transform: translateY(-8px);
        box-shadow: var(--card-shadow-hover);
      }
      
      .card-img-container {
        height: 200px;
        overflow: hidden;
        position: relative;
      }
      
      .card-img-top {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s ease;
      }
      
      .blog-card:hover .card-img-top {
        transform: scale(1.05);
      }
      
      .card-body {
        padding: 1.5rem;
        flex-grow: 1;
        display: flex;
        flex-direction: column;
      }
      
      .card-title {
        font-size: 1.25rem;
        font-weight: 600;
        margin-bottom: 0.75rem;
        color: #212529;
        line-height: 1.4;
        display: -webkit-box;
        /* -webkit-line-clamp: 2; */
        -webkit-box-orient: vertical;
        overflow: hidden;
      }
      
      .card-text {
        color: #6c757d;
        margin-bottom: 1.5rem;
        flex-grow: 1;
        display: -webkit-box;
        /* -webkit-line-clamp: 3; */
        -webkit-box-orient: vertical;
        overflow: hidden;
      }
      
      .btn-view {
        background: var(--primary-color);
        color: white;
        padding: 0.6rem 1.5rem;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 500;
        transition: all 0.3s;
        display: inline-block;
        align-self: flex-start;
        border: none;
        cursor: pointer;
      }
      
      .btn-view:hover {
        background: var(--primary-hover);
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(67, 97, 238, 0.3);
        color: white;
        text-decoration: none;
      }
      
      .empty-state {
        text-align: center;
        padding: 4rem 2rem;
        color: #6c757d;
        max-width: 600px;
        margin: 0 auto;
      }
      
      .empty-state h3 {
        margin-bottom: 1rem;
        color: #495057;
      }
      
      .empty-state p {
        margin-bottom: 2rem;
      }
      
      .btn-create {
        background: var(--primary-color);
        color: white;
        padding: 0.75rem 2rem;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 500;
        transition: all 0.3s;
        display: inline-block;
      }
      
      .btn-create:hover {
        background: var(--primary-hover);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(67, 97, 238, 0.3);
        color: white;
        text-decoration: none;
      }
      
      /* Animation for cards */
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      
      .blog-card {
        animation: fadeInUp 0.5s ease forwards;
        opacity: 0;
      }
      
      .blog-card:nth-child(1) { animation-delay: 0.1s; }
      .blog-card:nth-child(2) { animation-delay: 0.2s; }
      .blog-card:nth-child(3) { animation-delay: 0.3s; }
      .blog-card:nth-child(4) { animation-delay: 0.4s; }
      .blog-card:nth-child(5) { animation-delay: 0.5s; }
      .blog-card:nth-child(6) { animation-delay: 0.6s; }
      .blog-card:nth-child(7) { animation-delay: 0.7s; }
      .blog-card:nth-child(8) { animation-delay: 0.8s; }
      
      /* Responsive adjustments */
      @media (max-width: 768px) {
        .blog-grid {
          grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
          gap: 1.5rem;
        }
        
        .page-header {
          margin: 2rem 0;
        }
      }
      
      @media (max-width: 576px) {
        .blog-grid {
          grid-template-columns: 1fr;
          gap: 1.5rem;
          max-width: 400px;
        }
        
        .card-img-container {
          height: 180px;
        }
      }
    </style>
  </head>
  <body>
    <%- include('./partials/nav') %>

    <div class="page-header">
      <h1>Explore Our Blogs</h1>
      <p>Discover insightful articles, creative ideas, and inspiring stories from our community</p>
    </div>

    <div class="blogs-container">
      <% if (blogs.length > 0) { %>
        <div class="blog-grid">
          <% blogs.forEach(blog => { %>
          <div class="blog-card">
            <div class="card-img-container">
              <img src="<%= blog.coverImageURL %>" class="card-img-top" alt="<%= blog.title %>" />
            </div>
            <div class="card-body">
              <h5 class="card-title"><%= blog.title %></h5>
              <p class="card-text">
                <% if (blog.content && blog.content.length > 150) { %>
                  <%= blog.content.substring(0, 150) %>...
                <% } else if (blog.content) { %>
                  <%= blog.content %>
                <% } else { %>
                  No content available for this blog post.
                <% } %>
              </p>
              <a href="/blog/<%= blog._id %>" class="btn-view">Read More</a>
            </div>
          </div>
          <% }) %>
        </div>
      <% } else { %>
        <div class="empty-state">
          <h3>No blogs yet</h3>
          <p>Be the first to create an amazing blog post and share it with our community.</p>
          <a href="/blog/create" class="btn-create">Create Your First Blog</a>
        </div>
      <% } %>
    </div>

    <%- include('./partials/scripts') %>
  </body>
</html>